from setuptools import setup

setup(
    name='model_track',
    version='0.0.1.0',
    author='Quinton Barnes',
    description='A package for tracking diecast model progress.',
    packages=['model_track'],
    package_dir={'': 'src'}
)
